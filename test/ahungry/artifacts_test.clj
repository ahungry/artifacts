(ns ahungry.artifacts-test
  (:require [clojure.test :refer :all]
            ;; [ahungry.artifacts :refer :all]
            ))

(deftest test-stub
  (testing "Just some basic thing"
    (is (= 1 1))))
